---
title: shelltutor Profile Discovery — 2026-05-21
category: audit
component: profile-discovery
status: active
version: 0.1.0
last_updated: 2026-05-21
tags: [audit, profile, first-snapshot, agentic-architecture-audit-v3.1]
priority: high
---

# Profile Discovery — shelltutor — 2026-05-21

## 1. Snapshot Metadata

| Field | Value |
| --- | --- |
| Profile version | v1.2 |
| Directive version | project-profile-directive-v1.2 (2026-05-08) |
| Audit spec target | agentic-audit-spec-v3.1 (2026-05-08) |
| Snapshot date | 2026-05-21 |
| Generated at | 2026-05-21T16:45:00-08:00 |
| Generated by | shelltutor audit agent (Claude Code, Opus 4.7) |
| Profile mode | first-profile |
| Previous profile | none |
| Repository revision | git @ `e6257aa420b037139764c1f8f213853bd2574b92` on branch `main` |
| Remote | `git@github.com:jefahnierocks/shelltutor.git` |
| Source confidence | high |

The profile was produced against the working tree at revision `e6257aa`
(commit `refactor: remove user-specific accent branding from tutor`,
2026-05-21). Git working tree was clean at the start of profile execution.

## 2. Phase-by-phase Summary

### Phase A — Repository / Workspace Survey

- Root identity confirmed: `/Users/verlyn13/Organizations/jefahnierocks/shelltutor`.
- VCS: git. Three commits in history (`e402b58` chore: initialize project shell;
  `f69389e` feat: import shelltutor script from fedora-top prior-art;
  `e6257aa` refactor: remove user-specific accent branding from tutor).
- Only branch in the local clone is `main`; `origin/HEAD` is not set locally
  and no network access was attempted, so the GitHub remote default branch
  is recorded as `main` on the strength of the local-only branch evidence
  and the project's documentation posture.
- Top-level files (9 tracked): `shelltutor`, `README.md`, `AGENTS.md`,
  `CLAUDE.md`, `CONTRIBUTING.md`, `STATUS.md`, `ROADMAP.md`, `.editorconfig`,
  `.gitignore`. No build manifests, no package files, no lockfiles, no
  workspace declarations, no service or infrastructure manifests.
- Exactly one deployable unit: the executable `shelltutor` bash script
  (459 lines), kind `cli`, runtime `bash 4+`.
- No prior profile and no cycle history. Audit-package companion files
  are placed in this snapshot run (see §12 below).

### Phase B — Stack and Runtime Fingerprinting

- **Primary language**: bash (executable script).
- **Supporting language**: markdown (documentation set).
- **Runtime**: `bash 4+`. macOS ships bash 3.2 by default; README documents
  the upgrade path via Homebrew (README.md:53-55).
- **No** frameworks of any role: no HTTP/RPC framework, no ORM, no validation
  library, no UI framework, no model-provider SDK, no agent or subagent
  framework, no MCP/A2A SDK, no observability library, no eval framework,
  no IaC.
- **No** package manager, build tool, test framework, or eval framework.
- **Optional**: `shellcheck` is mentioned in `CONTRIBUTING.md:34-38` as
  "if available" and is recorded as known debt (static-analysis pass not
  yet completed; ROADMAP Phase 2 names it as exit criterion).

### Phase C — Infrastructure and Persistence Discovery

- **No** data stores, caches, queues, workflow engines, durable-execution
  workers, background-execution paths, callback receivers, object stores,
  vector stores, retrieval pipelines, external services, model providers,
  secrets managers, observability backends, policy engines, computer-use
  harnesses, or browser-use harnesses.
- **Local development infrastructure** — one entry: the per-learner
  `shelltutor` sandbox at `$HOME/.shelltutor` (overridable via the
  `SHELLTUTOR_HOME` environment variable). The sandbox carries:
  - `poem.txt` — 4-line educational fixture (created if missing).
  - `numbers.txt` — `seq 1 50` educational fixture (created if missing).
  - `.shelltutor_history` — HISTFILE for the practice subshell (sandboxed
    so tutor history does not pollute the learner's real `~/.bash_history`).
  - `.progress` — single integer (next-lesson index); written via
    `save_progress` and removed when the finale runs.
- **Classification note**: the sandbox is recorded under
  `infrastructure.local_development_infrastructure` rather than
  `agent_surface.memory_surfaces_detected`. The directive's memory-class
  taxonomy (`request-local-ephemeral`, `session-state`,
  `durable-conversation-state`, `long-term-memory`, `retrieval-corpus`,
  `retrieval-index`, `artifact-store`, `operator-rules-memory`,
  `checkpoint-state`) targets agent-runtime memory surfaces. The sandbox
  is per-learner CLI tutor state with no model-mediated reads or writes,
  so recording it as agent memory would be a category error. Deletion
  path is documented in the YAML: removing `$HOME/.shelltutor` (or
  `$SHELLTUTOR_HOME`) resets all state and `setup_sandbox()` re-creates
  fixtures on the next run.

### Phase D — Conventions, Governance, Rules

Twenty-one rules extracted verbatim from `AGENTS.md`, `CONTRIBUTING.md`,
`STATUS.md`, and `CLAUDE.md` (see `conventions.rules` in the YAML for the
full list with line citations). Highlights:

- **Forbidden** — tutor must not write outside its working directory,
  request elevated privileges, or reach the network (AGENTS.md:87-88).
  Tutor must not assume a particular operator, host, distro, or shell
  theme (AGENTS.md:89-90). Dependencies outside bash + a standard POSIX
  userland are restricted (AGENTS.md:75-78).
- **Required** — lessons must run end-to-end on a clean Linux or macOS
  terminal without prerequisite installation beyond bash
  (AGENTS.md:91-92). Tutor must be runnable via `./shelltutor` for
  verification (AGENTS.md:96-97).
- **Process** — Conventional Commits, one coherent change per commit,
  linear history, no merge commits (AGENTS.md:83-86; CONTRIBUTING.md:19-22).
  Prior-art imports must cite source path and commit SHA in the commit
  body (CONTRIBUTING.md:59-60).
- **Precedence** — closest local rule wins; if `STATUS.md`, `ROADMAP.md`,
  and `CONTRIBUTING.md` disagree, fix it in `STATUS.md` first
  (STATUS.md:65-69). When instructions conflict, prefer the most specific
  repo-local governance file (CLAUDE.md:19-22, AGENTS.md:29-30). The
  Jefahnierocks workspace boundary applies but rules are restated on
  shelltutor's own authority (AGENTS.md:21-30).
- **Conditional** — if a lesson genuinely requires a non-portable surface,
  gate it on a runtime check and degrade gracefully; do not couple the
  default path to the non-portable surface (CONTRIBUTING.md:51-54).

**Contradiction check** — one drift case, not a halt:

- `STATUS.md:31-46` and `ROADMAP.md:30-46` describe the user-agnostic
  refactor as a pending immediate next step, but commit `e6257aa` shows it
  has been performed and the script currently contains no `WYN OPS` accent
  comments. Recorded as audit-attention flag
  `drift-status-roadmap-vs-commit` for audit Phase 1; not a halt condition
  per directive §3 because the contradiction is between documentation and
  history, not between two governance rules on the same topic.

**No** ADRs exist. ADR template structure (does-it-include `Does not decide`)
is recorded `unknown` because there are no ADRs to inspect — the field
will become load-bearing only when the project introduces an ADR practice
(ROADMAP does not currently call for one).

**No** CI workflows, `CODEOWNERS`, PR templates, or branch protection by
intentional posture — CONTRIBUTING.md:72-75 declares local commit
discipline as the enforcement layer until a real activation trigger
appears. Known debt.

### Phase E — Agent, Automation, and Prompt Surface Inventory

This phase recorded an explicit-absent surface across every agentic
category. Each absence carries a search note in the YAML:

| Surface | Present | Search notes |
| --- | --- | --- |
| Model call sites | no | `grep -nE 'mcp|@anthropic|openai|claude-|gpt-|gemini|sampling|elicitation'` across all tracked files: 1 match (`component: claude-instructions` in CLAUDE.md frontmatter — file-role label, not a runtime model surface). No SDK imports, no client stubs. |
| Agent frameworks | no | No `langchain`, `llamaindex`, `crewai`, `autogen`, `swarm`, `haystack` references; no custom model loops. |
| Tools (agent sense) | no (count 0) | Script defines bash functions `next/prev/show/quit/exit` (shelltutor:126-130) — these are CLI navigation, not agent tools. No tool registry. |
| MCP | absent | No `mcp`, `modelcontextprotocol` references; no server/client stubs. |
| A2A | absent | No `@a2a`, `agent-card`, `advertised`, `task-state` references; no Agent Card files. |
| Workflow contracts | absent | No OpenAPI/AsyncAPI/Workflow Description schemas; no overlays. |
| Subagents | none | No subagent manifests; no dispatcher. |
| Resources / Resource templates | none | No MCP resources. |
| Prompts | none (count 0) | No model prompts. Script's heredoc lesson copy describes shell prompts, not model prompts; recorded as in-script lesson text under the script's evidence rather than as `documentation-about-prompts`. |
| Context assembly | none | No retrieval modules, memory assemblers, tool-description registries, or skill loaders. |
| Loops detected | none | The `for ((i=start; i<total; i++))` in `main()` (shelltutor:442-454) is a CLI flow loop with the lesson array as bound; not an LLM/agent loop. |
| Guardrails | none | No agent-runtime guardrails to detect. |
| Handoffs | none | No agent-to-agent handoff surface. |
| Memory surfaces | none (in agentic sense) | Sandbox `.progress` and history file are local CLI state; recorded under `infrastructure.local_development_infrastructure`. |
| Execution modes | none (in agentic sense) | Script's `main()` is a synchronous foreground loop. No background, scheduled, paused-for-approval, resumable, durable, callback-mediated, or event-triggered path. |
| Background agents | none | No cron/event/webhook/callback triggers. |

**Automation surface** — one entry: the `shelltutor` script itself,
classified `can_modify: artifacts` with the scope confined to `$SANDBOX`
via `setup_sandbox()` and the practice subshell rcfile (shelltutor:104-134).
Recorded with an `approval_hint` that captures the safety surface (no
elevated privileges, no network, no writes outside `$SANDBOX`).

### Phase F — Evals, Observability, Authority, Provenance, Versioning Baselines

- **Evals**: none. No `tests/`, `evals/`, `__tests__/`, or `*.test.*`
  files. CONTRIBUTING.md describes local verification ("Walk the affected
  lesson surface"; `shellcheck` "if available") but does not specify a
  behavior-quality eval suite. Suite count 0.
- **Observability**: none. No tracing, metrics, logs, OpenTelemetry, or
  semantic-convention references. Semantic-convention version/stability
  recorded `n/a` because the field is N/A, not unknown — there is nothing
  to pin.
- **Authority** — captured three approval gates from `AGENTS.md`:
  1. Scope-expansion outside the repository (human-in-loop, `ask`,
     no bypass) — AGENTS.md:43-47.
  2. Tutor safety-surface changes (writes, network, privilege, sandbox)
     (human-in-loop, `ask`, no bypass) — AGENTS.md:71-74.
  3. Introduce dependency outside bash + POSIX userland (`blocked`,
     `deny`) — AGENTS.md:75-78.
  Sandbox roots: `$HOME/.shelltutor` (default) and `$SHELLTUTOR_HOME`
  (operator override). Filesystem scope explicitly distinguishes the
  script's writes (confined to `$SANDBOX`) from the practice subshell's
  authority (real user shell — see audit-attention
  `subshell-safety-claim-vs-shell-authority`).
  `callback_authentication`, `secondary_credentials`,
  `hosted_or_local_agent_execution` are all N/A. `default_agent_authority`
  for coding agents operating on the repository is `sandboxed` (per
  AGENTS.md §Scope, §Authority Levels).
- **Provenance** — runtime/action emission `none`, content provenance
  `not-applicable` (no LLM-generated content), build/source provenance
  `absent` (no signed release yet; deferred per ROADMAP Phase 4).
  Repository carries Conventional Commits and a prior-art provenance
  commit (`f69389e`) in the commit body, but no SLSA/C2PA attestations.
- **Versioning** — http_apis, schemas, workflow_contracts,
  protocol_surfaces, semantic_conventions, prompts, tools, resources,
  subagents, provenance_records, eval_datasets all `n/a`. Policies and
  public_sdks `none` (semver `v0.1.0` tag intended but deferred).
- **Fitness functions**: none. CONTRIBUTING.md:72-75 names "local commit
  discipline" as the current enforcement layer; there is no CI rule,
  policy-as-code check, schema diff, dependency boundary check, prompt
  scan, or telemetry linter.

### Phase G — Operator Interview

One batched round was asked. Three questions; two answered, one left
unanswered. Recorded verbatim per directive §G:

- **Q1 — Strategic themes for audit weighting (multi-select)**: operator
  selected "Single-file portability" and "Documentation–code consistency".
  Recorded with default weighting (`dimensions: []`, `multiplier: 1.5`),
  per directive §5 strategic-themes notes ("if the operator supplies a
  theme name without a custom weighting, the audit applies the default
  mapping from §11.12 of `agentic-audit-spec-v3.1.md`").
- **Q2 — Audit cadence preference**: not answered by operator. Recorded
  as `unknown` per directive §5 schema. The §11 handoff checklist does
  not require cadence to be specified for first-profile readiness.
- **Q3 — Focus question for the audit**: operator selected
  "Does the sandbox-safety claim hold?" — referencing shelltutor:169 vs
  shelltutor:104-134. Recorded as `scope.focus_question` and as
  audit-attention flag `subshell-safety-claim-vs-shell-authority` for
  audit Phase 6.

No follow-up round is required; the unanswered cadence question is
declarable as `unknown` and does not block validation. Operator may
re-run Phase G as part of a future `focused-refresh` if cadence needs to
be set explicitly (directive §G allows strategic-theme and cadence
re-asking on refresh).

### Phase H — Profile Assembly and Validation

The profile YAML at `profile/2026-05-21/project_profile.yaml` was
assembled per the §5 schema. Validation pass against §7 follows in §13.

### Phase I — Profile Diff and Cycle-History Update

Not applicable — `meta.profile_mode == first-profile`, no prior profile
exists, no cycle history exists. Per directive §1.3 diff mode is only
mandatory when a previous profile exists. Per directive §I cycle history
is only appended with explicit operator approval; nothing to append at
first profile.

`profile-diff.md` and `profile-diff.json` are intentionally absent.
`profile/cycle-history.md` will be created the first time the operator
ratifies a cycle-history entry (no proposed entries yet).

## 3. Files and Directories Read

Repository (16 reads):

- `README.md`, `AGENTS.md`, `CLAUDE.md`, `CONTRIBUTING.md`, `STATUS.md`,
  `ROADMAP.md`, `shelltutor`, `.editorconfig`, `.gitignore`.

Audit-package companion docs (read once during scope expansion + copied
into `docs/audit/directives/agentic-architecture-audit-v3.1-package/`):

- `project-profile-directive.md` (authority; this directive)
- `audit-directive-set-manifest.md` (manifest)
- `audit-spec-friendly-explainer.md` (companion)
- `audit-kickoff-prompt.md` (companion)
- `agentic-audit-spec-v3.md` (header only; held for the audit run)
- `README.md` (audit-package overview)

## 4. Searches Performed and Limitations

Greps run across all tracked files (excluding `.git/`):

1. Agentic/model/protocol/observability markers — pattern
   `mcp|modelcontextprotocol|@anthropic|openai|claude-|gpt-|gemini|sampling|elicitation|otel|opentelemetry|temporal\.|inngest|trigger\.dev|langchain|llamaindex|crewai|autogen|swarm|haystack|@a2a|agent-card|webhook|callback|background|cron|schedule`.
   Result: one match (`component: claude-instructions` in CLAUDE.md
   frontmatter — file-role label, not a runtime surface).
2. Broader agentic / RAG / prompt / model patterns — pattern
   `mcp|@anthropic|openai|anthropic|llm|agent|prompt|model|embed|vector|rag|retrieval|webhook|callback|cron|schedule|workflow|temporal|durable|otel|opentelemetry`.
   Result: matches are all about agents-as-instructions-files (governance),
   shell prompts (CLI), and prompt-theme assumptions (negative claim, do
   not assume). No runtime agentic surface.
3. Network markers — pattern
   `curl|wget|nc |netcat|ssh |http://|https://|fetch|api\.`.
   Result: no matches (the only URL-shaped text is in the audit-package
   README which lives outside scope).
4. Privilege markers — pattern `sudo|setuid|chmod \+s|root|elevate`.
   Result: matches are documentation forbidding privilege escalation
   (negative claims) and one in-lesson `sudo dnf install` hint in
   lesson 8 — recorded as `lesson-portability-gaps` audit-attention.
5. Eval / benchmark markers — pattern
   `eval|benchmark|golden|fixture|coverage`. Result: no matches.

Limitations:

- Default branch for the GitHub remote could not be confirmed from the
  local clone; `origin/HEAD` symbolic ref is not set locally and no
  network access was attempted. Recorded `main` on the strength of the
  local-only branch evidence and the project's posture documentation.
- Audit cadence preference was not answered in Phase G; recorded as
  `unknown`.
- Lesson-level portability claims (lessons 7 and 8) are captured as
  audit-attention rather than verified by running the tutor on macOS
  during profile execution — verification belongs to the audit, not the
  profile.

## 5. Operator Interview Answers (Verbatim)

| Question | Answer |
| --- | --- |
| Strategic themes for audit weighting (multi-select) | "Single-file portability, Documentation–code consistency" |
| Audit cadence preference | *(not answered; recorded as `unknown`)* |
| Focus question for the audit | "Does the sandbox-safety claim hold?" |

## 6. Claimed Bounded Contexts

None claimed. The project is a single bash script with one purpose
(interactive shell tutor). The directive's bounded-context concept
applies to subsystems with distinct vocabularies inside a larger codebase;
shelltutor has no such subsystem boundary. `claimed_bounded_contexts: []`
is the honest profile state per directive §5 (empty list is allowed).

The audit may still want to consider lesson-level vocabulary boundaries
(e.g., "user-agnostic" vs "Linux+macOS portable" — see
`lesson-portability-gaps` flag) under Phase 1 vocabulary.

## 7. Strategic Themes and Weighting Recommendations

Two themes selected by operator in Phase G:

1. **Single-file portability** — multiplier 1.5, dimensions deferred to
   audit §11.12 default mapping. Recommended audit weighting on Phase 4
   (contracts) and Phase 1 (vocabulary around "portable across Linux and
   macOS"). Supports `lesson-portability-gaps` flag triage.
2. **Documentation–code consistency** — multiplier 1.5, dimensions
   deferred. Recommended audit weighting on Phase 1 (vocabulary/claims)
   and Phase 8 (documentation as policy). Supports
   `drift-status-roadmap-vs-commit` flag triage.

Both themes are first-snapshot (no prior cycle).

## 8. Known Debt

Six entries, all `open`, sourced from `STATUS.md §Deferrals` and
`ROADMAP.md`:

1. LICENSE not chosen; repository public (STATUS.md:53-58).
2. CHANGELOG.md absent; no tagged release (STATUS.md:53-58).
3. GitHub-side automation intentionally omitted (CONTRIBUTING.md:72-75).
4. Portability validation pending on Linux distros and macOS bash 5+
   (ROADMAP.md:50-62).
5. `shellcheck` pass not yet recorded (ROADMAP.md:58-62, CONTRIBUTING.md:34-38).
6. Lesson surface review pending; lessons not yet jump-addressable from the
   menu (ROADMAP.md:66-82).

## 9. Audit-attention Flags → Target Audit Phase

| ID | Description (short) | Target Phase | Reason |
| --- | --- | --- | --- |
| `drift-status-roadmap-vs-commit` | STATUS/ROADMAP claim user-agnostic refactor pending; commit `e6257aa` shows it complete. | 1 (vocabulary/claims) | Documentation claims contradicted by source state; aligns with documentation–code consistency theme. |
| `subshell-safety-claim-vs-shell-authority` | "You cannot break anything" claim broader than the actual sandbox; subshell holds learner's normal user authority. | 6 (authority) | Matches operator focus question; sandbox/authority scope claim mismatch. |
| `lesson-portability-gaps` | Lesson 7 uses `/proc` and `free -h` (Linux-only); Lesson 8 uses `sudo dnf install` (Fedora-only) under a Linux+macOS portability claim. | 1 (vocabulary/claims) | Aligns with single-file portability theme. |
| `agents-md-parent-reference-style` | Parent AGENTS.md cited by absolute machine-local path; rules are restated locally so the reference is informational. | 4 (contracts) | Minor companion-doc reference style edge. |

## 10. Drift Against Prior Profile

None — this is the first profile. `profile-diff.md` and `profile-diff.json`
are intentionally absent.

## 11. Protocol-Surface, Authority-Matrix, State/Memory, Provenance, and Semantic-Convention Baseline Notes

**Protocol surfaces** — explicit-absent. MCP, A2A, workflow-description,
overlay, and schema-dialect surfaces are all not present. Search notes
in the YAML cite the patterns used. Per directive Appendix B: absent
surfaces are recorded with search notes rather than omitted.

**Authority matrix** — three approval gates recorded with explicit
`approval_mode`, empty `bypass_modes`, and precedence hints (see YAML
`baselines.authority.approval_gates`). All gates source from `AGENTS.md`
and apply to coding agents and to operators making tutor safety-surface
changes; none apply to a model-mediated runtime (there is none).
`hosted_or_local_agent_execution: n/a`, `callback_authentication: n/a`,
`secondary_credentials: []`.

**State and memory** — the directive's nine memory classes
(`request-local-ephemeral`, `session-state`, `durable-conversation-state`,
`long-term-memory`, `retrieval-corpus`, `retrieval-index`, `artifact-store`,
`operator-rules-memory`, `checkpoint-state`) are all unpopulated.
`agent_surface.memory_surfaces_detected: []`. The sandbox `.progress`
file and the `.shelltutor_history` file are recorded under
`infrastructure.local_development_infrastructure` instead — they are
per-learner CLI state, not agent memory. Deletion path: remove
`$HOME/.shelltutor` (or `$SHELLTUTOR_HOME`); `setup_sandbox()`
re-creates the fixtures on next run.

**Provenance** — three classes recorded separately per directive Appendix
B: runtime/action emission `none`, content provenance `not-applicable`,
build/source provenance `absent`. No SLSA / C2PA / signed-release
attestations; Conventional Commits and one prior-art provenance commit
(`f69389e`) provide commit-level traceability but do not satisfy any of
the three directive provenance classes.

**Semantic conventions** — `n/a` rather than `unknown`. There is no
tracing or telemetry to pin to a convention version. The field is N/A
because the underlying capability is absent, not because the version is
unknown.

## 12. Companion-Artifact Alignment Notes

The audit directive set is now in-repo at
`docs/audit/directives/agentic-architecture-audit-v3.1-package/` (copied
from `~/Downloads/audit spec v3 2/agentic-architecture-audit-v3.1-package/`
during scope expansion authorized by the operator's invocation). All six
companion files target the same authority versions
(profile directive v1.2 / audit spec v3.1, both dated 2026-05-08), so
`drift_status: aligned` is recorded for each.

The decision was to copy rather than reference for three reasons:

1. **Validation traceability** — directive §7 requires
   `governance.companion_artifacts` to track `targets_spec_version` and
   `drift_status`. References into `~/Downloads/` are machine-local and
   would defeat the §7 check on any clone of the repository.
2. **Reproducibility** — the audit-kickoff prompt instructs the audit
   agent to read the spec file beside `audit-directive-set-manifest.md`
   ("If `audit-directive-set-manifest.md` exists beside the spec, read
   it after the spec and before preflight."). Co-locating the package
   inside the repository makes this a project-local read, not a
   scope-expanded one.
3. **Snapshot semantics** — directive v1.2 dated 2026-05-08 is a fixed
   point; copying preserves the snapshot at the path the profile cites,
   independent of changes to the user's `~/Downloads/` directory.

The audit spec body itself was not exhaustively read during profile
execution — only the header (version banner) was checked to confirm
v3.1 / 2026-05-08. The audit agent must read the full spec in Phase 0
per the kickoff prompt.

## 13. Validation Checklist (§7 Pass)

Each §7 rejection condition is evaluated below:

- ✅ No required field is null/empty without `source: unknown` and a reason.
- ✅ All file-cited fields carry path and line range.
- ✅ Every framework/stack role lacks evidence only if the role is absent
  (empty lists are valid; search notes appear in the YAML and in §4 above).
- ✅ Every rule in `conventions.rules[]` carries source metadata
  (path + lines).
- ✅ No `claimed_bounded_contexts[]` entry exists; the empty list is
  honest per Phase A evidence.
- ✅ Contradictory rules: none on the same topic. The single drift case
  (STATUS/ROADMAP vs commit log) is routed as audit-attention flag
  `drift-status-roadmap-vs-commit` for Phase 1, not as a halt.
- ✅ Infrastructure access rules: explicit (sandbox roots, filesystem
  scope) with no initial-violation search required because the
  `shelltutor` script writes only inside `$SANDBOX` (confirmed by reading
  shelltutor:30, 67, 69, 76-79, 83, 109).
- ✅ `agent_surface.tools.count == 0`; schema status `absent` with reason
  "no tools exist".
- ✅ `agent_surface.protocol_surfaces.mcp.present == false`; concrete
  surface counts populated with explicit zeros and search notes.
- ✅ `agent_surface.protocol_surfaces.a2a.present == false`; advertised
  skills/Agent Cards/auth schemes empty with search notes.
- ✅ `agent_surface.subagents.count == 0`; `typed_boundaries == false`.
- ✅ `agent_surface.prompts.count == 0`; surface-type counts all zero;
  search notes explain why heredoc lesson text is not a prompt surface.
- ✅ `agent_surface.memory_surfaces_detected: []`; sandbox state is
  recorded under `infrastructure.local_development_infrastructure` with
  classification reasoning.
- ✅ `baselines.observability.semantic_conventions_used == none`; version
  and stability `n/a` (capability absent, not unknown).
- ✅ Approval gates carry approval_mode, bypass_modes, precedence_hint,
  and evidence.
- ✅ `governance.companion_artifacts` all carry `targets_spec_version`
  and `drift_status: aligned`. No companion targets a different spec
  version, so no audit-attention flag is required for companion drift.
- ✅ No previous profile exists; diff artifacts are intentionally absent
  per directive §1.3.
- ✅ All audit-attention flags carry `target_audit_phase`.
- ✅ No operator-interview answer overwrites stronger file evidence (the
  themes and focus question come from operator only because they cannot
  be derived from files).
- ✅ No cycle-history additions written without operator approval (none
  exist).
- ✅ `strategic_themes` non-empty entries carry `weighting` (default
  `multiplier: 1.5`, `dimensions: []`) and `source: operator-interview`.

All §7 checks pass.

## 14. Halt History

No halts triggered. None of the §3 halt conditions were met:

- Two governance files contradicting each other on the same topic: no
  (STATUS vs commit-log drift is documentation vs history, not two
  governance rules on the same topic).
- No readable anchors: no (README and AGENTS.md are clear).
- Multiple unnamed projects in one path: no (one project).
- Manifest-versus-code framework contradiction: no (no manifests).
- ADR supersession chain unable to linearize: no (no ADRs).
- Scope includes unreadable regions: no (everything in scope is read).
- Prior profile disagrees on root identity: no (no prior profile).
- Strategic theme referencing absent structural concern: no (themes
  match present evidence).
- Visible protocol surface with unprofileable capabilities: no (no
  protocol surfaces present).
- Companion-artifact authority version conflict: no (all aligned).

## 15. Cycle-History Proposed Updates

None. Cycle history starts empty at first profile. No proposed
conventions to ratify yet.

## 16. Diff Summary

Not applicable — first profile.

---

## Handoff Readiness (Directive §11)

| Check | Status |
| --- | --- |
| `project_profile.yaml` exists and validates against §7 | ✅ |
| `profile-discovery.md` exists | ✅ |
| `profile-diff.md` / `profile-diff.json` if previous profile exists | N/A (first profile) |
| `audit_attention_flags[]` routed with target audit phases | ✅ (4 flags, Phases 1/4/6) |
| `claimed_bounded_contexts[]` remain `status: claimed` | ✅ (empty list, honest absence) |
| Protocol surfaces inventoried by concrete type or explicit-absent + search notes | ✅ |
| Authority baselines include approval mode, bypass, precedence, callback/secondary-credential notes | ✅ (callback/secondary `n/a` with reason) |
| State and memory surfaces distinguish nine classes where evidence exists | ✅ (none populated; sandbox under `local_development_infrastructure`) |
| Semantic-convention version and stability recorded when observability is claimed | ✅ (`n/a` because observability absent) |
| Provenance distinguishes runtime/action, content, build/source | ✅ (`none` / `not-applicable` / `absent`) |
| Scope include/exclude paths explicit | ✅ |
| Strategic themes present or explicitly absent | ✅ (two themes) |
| Previous cycle conventions available via `governance.cycle_history_path` or explicitly absent | ✅ (`none`) |
| Halt conditions resolved or audit intentionally not started | ✅ (no halts) |

**Profile ready for audit kickoff per directive §11.** The audit agent
should read
`docs/audit/directives/agentic-architecture-audit-v3.1-package/agentic-audit-spec-v3.md`
in full, then
`docs/audit/directives/agentic-architecture-audit-v3.1-package/audit-directive-set-manifest.md`,
then begin Phase 0 of the audit with `profile/2026-05-21/project_profile.yaml`
and this discovery as inputs.

---
title: shelltutor Audit — Phase 10.5 Finding Smoke-Test
category: audit
component: phase-10-5
status: active
version: 0.1.0
last_updated: 2026-05-21
tags: [audit, phase-10-5, smoke-test]
priority: high
---

# Phase 10.5 — Finding Smoke-Test

The audit runs against snapshot revision
`e6257aa420b037139764c1f8f213853bd2574b92` and the audit completes
**on the same day, against the same revision** (`git rev-parse HEAD`
at smoke-test time returned the same SHA; the only untracked items
are `audit/`, `docs/`, `profile/` — audit infrastructure, not source
changes).

Every finding is therefore expected to smoke-test
`confirmed-current`. The phase nonetheless re-verifies each citation
against the working tree at HEAD because the directive treats the
smoke-test as non-skippable: drift between snapshot and current can
also be introduced by typos or by miscounted lines.

## Working-tree state at smoke-test

```text
$ git rev-parse HEAD
e6257aa420b037139764c1f8f213853bd2574b92
$ git status --short
?? audit/
?? docs/
?? profile/
```

Three untracked top-level directories, all audit/profile infrastructure
introduced during this cycle. Zero tracked-file changes since the
snapshot.

## Per-finding outcomes

### F-001 — `confirmed-current`

Re-verified four key citations:

| Citation | Re-read result |
| --- | --- |
| `STATUS.md:33-34` | `- The imported script still carries two \`WYN OPS\` accent comments` + `(lines 17 and 35); user-agnostic refactor is the next change.` ✅ matches snapshot evidence |
| `STATUS.md:41-43` | `1. **User-agnostic refactor commit** — replace \`WYN OPS\` accent comments` + `with neutral wording; sweep for any other user-specific framing.` ✅ |
| `ROADMAP.md:37` | `- Replace \`WYN OPS\` accent comments with neutral language.` ✅ |
| `shelltutor:17` | `#   - Title + action footer styled with a single accent color.` ✅ (no WYN OPS) |
| `shelltutor:35` | `    H1=$'\e[1;38;5;208m'      # lesson title — bold orange accent` ✅ (no WYN OPS) |
| `grep WYN OPS shelltutor` | exit=1 (zero matches) ✅ |

Outcome: `confirmed-current`.

### F-002 — `confirmed-current`

| Citation | Re-read result |
| --- | --- |
| `shelltutor:169` | `Everything happens in ${DIR}~/.shelltutor${RST}; you cannot break anything.` ✅ |
| `shelltutor:104-134` | `practice()` body unchanged: `bash --rcfile <(cat <<RCFILE ... cd "$SANDBOX"; HISTFILE="$SANDBOX/.shelltutor_history"; ...; PS1='shelltutor> '; next/prev/show/quit/exit overrides ... RCFILE) -i < /dev/tty` ✅ |
| `AGENTS.md:75-78` | bash + POSIX userland dependency rule ✅ |

Outcome: `confirmed-current`.

### F-003 — `confirmed-current`

| Citation | Re-read result |
| --- | --- |
| `shelltutor:330` | `${SUB}${DIR}/proc${RST}${SUB}${RST} is Linux's self-report folder. Files are made up` ✅ |
| `shelltutor:344` | `any that says 'command not found'. To install:` ✅ |
| `shelltutor:346` | `  ${CMD}sudo dnf install cowsay figlet lolcat${RST}` ✅ |
| `README.md:50-63` | Linux + macOS portability claim ✅ |
| `AGENTS.md:91-92` | "Lessons must be runnable end-to-end on a clean Linux or macOS terminal..." ✅ |
| `CONTRIBUTING.md:51-54` | Gate-and-fallback conditional rule ✅ |

Outcome: `confirmed-current`.

### F-004 — `confirmed-current`

| Citation | Re-read result |
| --- | --- |
| `AGENTS.md:75-78` + `87-92` | Safety rules ✅ |
| `CONTRIBUTING.md:72-75` | `Following the Jefahnierocks workspace posture, this repository intentionally / omits CI workflows, CODEOWNERS, PR templates, branch protection, and / Actions secrets until a real activation trigger exists. Until then, local / commit discipline is the enforcement layer.` ✅ |
| `.git/hooks/` | Only `*.sample` files (the bash-shipped defaults); no project-tracked hooks ✅ |
| `.github/` | absent ✅ |
| `.githooks/` | absent ✅ |

Outcome: `confirmed-current`.

### F-005 — `confirmed-current`

| Citation | Re-read result |
| --- | --- |
| `AGENTS.md:21-23` | `The Jefahnierocks workspace contract at` + `/Users/verlyn13/Organizations/jefahnierocks/AGENTS.md\` also applies inside` + `this repository. ...` ✅ |
| `AGENTS.md:24-30` | restatement clause unchanged ✅ |

Outcome: `confirmed-current`.

### F-006 — `confirmed-current`

| Citation | Re-read result |
| --- | --- |
| `find . -name '*test*' …` | returns only `audit/2026-05-21/09-evals.{md,json}` (audit infrastructure); no project-tracked tests ✅ |
| `ROADMAP.md:50-65` | Phase 2 portability / shellcheck pending ✅ |
| `CONTRIBUTING.md:34-38` | shellcheck "if available" guidance ✅ |

Outcome: `confirmed-current`.

### F-007 — `confirmed-current`

| Citation | Re-read result |
| --- | --- |
| `audit/2026-05-21/04-contracts.json` | six contracts, all `ad-hoc` ✅ (the audit infrastructure cites itself; this is acceptable because the contract inventory is what F-007 cites about the current state) |
| `shelltutor:53-64` | `usage()` heredoc unchanged ✅ |

Outcome: `confirmed-current`.

## Per-fitness-function reality check

Per the directive Phase 10.5 procedure for FFs: "Verify the proposed
enforcement tech is currently installed or installable in the
project's CI. If a fitness function would be unenforceable, demote to
`manual-review-only`."

shelltutor has no CI (intentional posture per CONTRIBUTING.md:72-75).
The proposed FFs are enforceable as **pre-commit hooks** or **Makefile
targets** or **manual-review checklists**, all of which the project's
bash-only + POSIX-userland constraint permits.

| FF | Enforcement tech | Status | Note |
| --- | --- | --- | --- |
| FF-001 | bash + grep | installed | trivial; no demotion |
| FF-002 | bash + grep | installed | trivial; no demotion |
| FF-003 | bash + grep + git | installed | trivial; no demotion (initial manual-review-only per FF text) |
| FF-004 | bash + grep | installed | no demotion (initial manual-review-only per FF text) |
| FF-005 | shellcheck (not installed) | installable | `brew install shellcheck` / `apt install shellcheck` / `dnf install ShellCheck` — outside the bash-only runtime constraint but acceptable as a contributor tool per CONTRIBUTING.md:34-38; no demotion |
| FF-006 | pty harness (expect / python pty / socat) | not installed; potentially **outside** AGENTS.md:75-78 runtime constraint | **demoted** to `manual-review-only` for full lesson-flow; the minimal `-h` smoke variant stays as `static-analysis` (just bash + grep) per FF-006's documented demotion path |
| FF-007 | bash + grep | installed | trivial; no demotion |

FF-006 demotion is consistent with its own documented contingency plan.

## Summary

| Finding | Smoke-test outcome |
| --- | --- |
| F-001 | confirmed-current |
| F-002 | confirmed-current |
| F-003 | confirmed-current |
| F-004 | confirmed-current |
| F-005 | confirmed-current |
| F-006 | confirmed-current |
| F-007 | confirmed-current |

All seven findings remain `active`. Zero `struck`, zero `drift-only`,
zero `not-run`. The `> 1/3 struck-or-drift-only` halt condition (§4.1)
does **not** apply.

FF-001..FF-007 all remain `active` candidates; FF-006 carries an
internal demotion of its full lesson-flow variant to `manual-review-only`
per its own implementation notes, with the minimal `-h` variant
retained as `static-analysis`.

## Boundary Declarations Honored (Phase 10.5)

- Did not erase original snapshot evidence — preserved verbatim in
  `10-findings.json`.
- Did not silently update line numbers — every re-read confirmed the
  cited line; no `re-evidenced` outcome was needed.
- Did not convert any failed smoke-test into a remediation claim
  (none failed).
- Did not use smoke-test to expand scope.
- Did not re-derive findings from scratch — re-verified only what was
  cited.

## Exit Check

| Check | Status |
| --- | --- |
| Every finding has a smoke-test outcome | ✅ all seven `confirmed-current` |
| Every `struck` finding absent from final priority ranking but retained in log | ✅ vacuous (none struck) |
| Every `drift-only` recorded as profile/code drift | ✅ vacuous (none drift-only) |
| Stale-rate halt check (> 1/3 struck-or-drift-only) | ✅ rate 0/7 → no halt |

Phase 10.5 exit check **passes**. Advancing to Phase 11 (`SUMMARY.md`).
